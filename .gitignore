# Python
__pycache__/
*.pyc
*.pyo
.venv/
backend/.venv/
*.egg-info/
.pytest_cache/

# DuckDB data store (regenerable)
backend/data/*.duckdb
backend/data/*.duckdb.wal

# Node / Vite
frontend/node_modules/
frontend/dist/
node_modules/

# Env
.env.local
.env.*.local

# Editor / OS
.DS_Store
.idea/
.vscode/

# Real customer book inputs (sensitive: real names/volumes; large) — dropped into sample_data/deals/
# by the operator and read by the repeatable loaders. Never committed.
backend/sample_data/deals/
